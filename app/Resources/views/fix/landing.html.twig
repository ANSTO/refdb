<div class="modal-header">
    <h5 class="modal-title">Fix a problem</h5>
    <button type="button" class="close" data-dismiss="modal" aria-label="Close">
        <span aria-hidden="true">&times;</span>
    </button>
</div>
<div class="modal-body">
    <p>Click on the area where the reference is incorrect.</p>
    <p>
        <a href="reference">{{ reference.titleSection|latin }}</a>
        <a class="btn-modal" href="{{ path("fix_conference", {"id": reference.id }) }}">{{ reference.conferenceSection|latin }}</a>, <a href="reference">{{ reference.paperSection|latin }}</a>
    </p>
    <p>or</p>
    <p><a href="other">The whole reference is incorrect</a></p>
    <p>or</p>
    <p><a href="other">The LaTeX formatting is incorrect</a></p>
    <p>or</p>
    <p><a href="other">Add additional information to this reference</a></p>
    <br />
    <a href="javascript:void(0)" class="btn btn-default btn-block" data-dismiss="modal">Cancel</a>
</div>